# Matrix Bot Credentials
BOT_USER=@invitebot:localhost
BOT_PASSWORD=strong_password_here
HOMESERVER_URL=http://localhost:8008

# Backend API Configuration
BACKEND_URL=http://localhost:3000
BACKEND_ADMIN_API_KEY=your_backend_api_key_here

# Authorized Admin Users (comma-separated Matrix user IDs)
# Only these users can use the !invite command
ALLOWED_ADMINS=@admin:localhost

# Optional: Synapse Admin Token (for dynamic admin checking)
# If set, bot will verify admin status via Synapse API instead of ALLOWED_ADMINS list
# SYNAPSE_ADMIN_TOKEN=syt_xxxxx
